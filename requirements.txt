# LLM APIs
anthropic
aider-chat
backoff
openai
requests
# Novelty engines
pyalex            # --engine openalex
# Console / REPL
rich
questionary
prompt_toolkit
# Viz
matplotlib
pypdf
pymupdf4llm
# Common Requirements
torch
numpy>=1.26          # floors below steer pip's resolver away from pre-cp312 sdists
transformers>=4.40
datasets>=2.18
tiktoken>=0.5        # (aider backtracking otherwise lands on 0.4.0 / numpy 1.24.3 / aiohttp 3.8.4
aiohttp>=3.9         #  — all of which fail to build on Python 3.12)
wandb
tqdm
